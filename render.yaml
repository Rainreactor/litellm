services:
  - type: web
    name: openai-proxy
    plan: free # MANDATORY for free tier
    runtime: image
    image:
      url: ghcr.io/berriai/litellm:main-stable
    envVars:
      - key: PORT
        value: "4000" # Use quotes for values to avoid YAML parsing issues
    autoDeploy: true
